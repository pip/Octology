#!/usr/bin/perl
use strict;use warnings;use utf8;use Octology::a8; # insert Whitez 4 `coma` positionz in 4wRd d8ColorString if comma Dtected in input, but doesn't handle ...
d8cs('WRoYGWCBMp') if(@ARGV && $ARGV[0]=~ /,/);o8(bfr8c(@ARGV)); #  ... piping commaz thru STDIN yet, which `b8clr` should handle pretty well though instead;
