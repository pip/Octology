#!/usr/bin/perl
use strict;use warnings;use utf8;use Octology::a8;
my $a8o=Octology::a8->new(@ARGV);o8($a8o->a8c()); # default new a8 object loads ~/.Hrc but can take a parameter filename for loading && coloring different d8a;
