#!/usr/bin/perl
use strict;use warnings;use utf8;use Octology::a8;o8( chti(@ARGV));
# basic CHange window && icon TItle function using XTerm dtterm control sequence:
#   OSC Ps;Pt ST ...like "\e\]\d;NwTi\e\\" with \d being just a [012] param where 0=both, 1=IconName, 2=WindowTitle;
